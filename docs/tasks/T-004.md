# T-004: Process management module

- **Status**: DONE
- **Milestone**: 2 — Core Modules
- **Depends**: T-000
- **PRD Reference**: §3.2, §3.4
- **Completed**: 2026-03-10 18:29
- **Commit**: b4b483766326aab841b04d9f9c8fe63dbdc70810
- **Cost**: $0.47

## Description

Manage child processes for ralph — primarily spawning and monitoring Claude CLI sessions, but also handling process tree cleanup.

**Capabilities:**

- Spawn a child process and capture output to a file
- Kill a process tree (process + all descendants)
- Find processes by command pattern (for kill command)
- Monitor a running process (detect completion, timeout)

Process management must handle graceful shutdown (SIGTERM first, then SIGKILL after timeout) and proper cleanup on parent exit.

## Produces

- `src/ralph/core/process.ts`
- Tests covering: spawn and capture, process tree kill, timeout behavior

## Completion Notes

Implemented process management module in `src/ralph/core/process.ts` with 4 exported functions: `spawnWithCapture()` (spawn with stdout/stderr capture to log file), `killProcessTree()` (SIGTERM then SIGKILL after grace period), `findProcessesByPattern()` (find PIDs by command pattern via `ps`), `monitorProcess()` (track completion/timeout with optional output callback). 15 tests covering all capabilities including graceful shutdown, SIGTERM-resistant processes, process tree kill, timeout detection, and output callbacks.
