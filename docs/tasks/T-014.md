# T-014: CI workflow

- **Status**: DONE
- **Milestone**: 7 — CI/CD & Publishing
- **Depends**: T-000
- **PRD Reference**: §6
- **Completed**: 2026-03-10 21:12 (2m duration)
- **Commit**: ba93528b589ae4618abdcea09f7417a2067b4250
- **Cost**: $0.42

## Description

Create a GitHub Actions CI workflow that runs on push to `main` and on pull requests.

**Behavior:**

- Install pnpm and Node.js 20
- Run `pnpm install --frozen-lockfile`
- Run `pnpm check` (lint → format → typecheck → build → test:coverage)

## Produces

- `.github/workflows/ci.yml`
- Tests verifying the workflow file is valid YAML with expected structure

## Completion Notes

Created `.github/workflows/ci.yml` with GitHub Actions workflow that triggers on push to main and pull requests. Installs pnpm via `pnpm/action-setup@v4`, sets up Node.js 20 with pnpm cache, runs `pnpm install --frozen-lockfile`, then `pnpm check`. 11 tests verify YAML validity and expected workflow structure (triggers, runner, Node version, pnpm setup, install flags, check step, checkout order).
