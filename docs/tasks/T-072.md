# T-072: Rewrite README for developer onboarding

- **Status**: DONE
- **Milestone**: 12 — Documentation
- **Depends**: T-071
- **PRD Reference**: §1
- **Complexity**: small
- **Completed**: 2026-03-18 10:07 (2m duration)
- **Commit**: 16d4de604d0bdeff404e1f529a28dce242ccb63d
- **Cost**: $0.25

## Description

Rewrite the README to be genuinely helpful for a developer encountering ralph for the first time. The current README is a thin reference table — it doesn't explain what a PRD is in this context, how to write tasks, or how the loop actually works. A new user should be able to read the README and understand the full workflow end-to-end.

## Scope

- Expand the **Quick Start** section into a walkthrough that covers:
  1. Running `ralph init` and what it scaffolds
  2. Writing a PRD (`docs/PRD.md`) — what it should contain, how ralph uses it
  3. Writing task files (`docs/tasks/T-NNN.md`) — the required fields (Status, Milestone, Depends, Description, AC), how tasks relate to the PRD
  4. Running `ralph loop` and what happens (task selection → boot prompt → red/green TDD → commit → next task)
  5. Monitoring progress with `ralph monitor`
- **Remove** these commands from the commands table: `kill`, `milestones`, `shas`, `cost` (keep them in the full docs site, not the README)
- Keep the README concise — this is onboarding, not exhaustive docs. Link to the docs site for deep dives.
- Preserve existing sections: Why Ralph, Requirements, Documentation link

## AC

- [x] README explains the PRD → tasks → loop workflow clearly enough that a new developer can get started without reading other docs
- [x] Commands table only includes: `init`, `loop`, `monitor`, `retry`, `update`
- [x] No broken links
- [x] `pnpm check` passes

## Completion Notes

Rewrote README with a full "Getting Started" walkthrough covering: (1) `ralph init` and what it scaffolds, (2) writing a PRD with numbered sections, (3) creating task files with required fields explained, (4) running `ralph loop` with the 5-phase cycle explained, (5) monitoring with `ralph monitor`. Removed `kill`, `milestones`, `shas`, and `cost` from the commands table. Preserved Why Ralph, Requirements, and Documentation sections.
