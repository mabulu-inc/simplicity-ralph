# T-050: Remove --no-db flag dead code

- **Status**: TODO
- **Milestone**: 3 — Hardening
- **Depends**: none
- **PRD Reference**: §3.2

## Description

The `--no-db` flag is parsed in `parseLoopOptions` but never used — no Docker container management was ever implemented. The PRD has been updated to remove Docker/database lifecycle references from `ralph loop`. Now remove the dead code from the implementation:

- Remove `db` from `LoopOptions` interface
- Remove `--no-db` case from `parseLoopOptions` switch
- Remove `db` default from options object
- Remove `db` line from `formatDryRunConfig` output
- Remove `--no-db` test case from `loop.test.ts`
- Update the defaults assertion and multi-option assertion to not include `db`

## Hints

All changes are in two files:

- `src/ralph/commands/loop/index.ts` — remove `db` from interface, defaults, switch case, and dry-run output
- `src/ralph/__tests__/loop.test.ts` — remove the `--no-db` test and `db` from assertion objects

## Produces

- `src/ralph/commands/loop/index.ts` (modified)
- `src/ralph/__tests__/loop.test.ts` (modified)
