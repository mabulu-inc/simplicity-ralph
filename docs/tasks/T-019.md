# T-019: Externalize pricing and complexity tier configuration

- **Status**: DONE
- **Milestone**: 8 — Robustness
- **Depends**: none
- **PRD Reference**: §8.2
- **Completed**: 2026-03-10 22:05 (5m duration)
- **Commit**: 07204ede9494ce54b5f842bcd25bfa5514f629cd
- **Cost**: $0.71

## Description

Move hardcoded operational parameters out of source code so they can be updated without rebuilding. Currently:

- `PRICE_INPUT`, `PRICE_CACHE_WRITE`, etc. are hardcoded in `commands/cost.ts`
- `TIER_SCALING` thresholds are hardcoded in `commands/loop.ts`

**Requirements:**

- Define sensible defaults in code (current values)
- Allow overrides via environment variables (e.g., `RALPH_PRICE_INPUT`) and/or a config file section
- Document the override mechanism

## Produces

- Updated `src/ralph/commands/cost.ts`
- Updated `src/ralph/commands/loop.ts`
- Config loading utility or extension to `src/ralph/core/config.ts`
- Tests for override behavior

## Completion Notes

Created `src/ralph/core/defaults.ts` with `getPricing()` and `getTierScaling()` functions that define sensible defaults and allow environment variable overrides:

**Pricing env vars:** `RALPH_PRICE_INPUT`, `RALPH_PRICE_CACHE_WRITE`, `RALPH_PRICE_CACHE_READ`, `RALPH_PRICE_OUTPUT`

**Tier scaling env vars:** `RALPH_TIER_{LIGHT,STANDARD,HEAVY}_{MAX_TURNS,TIMEOUT}`

Invalid, empty, zero, or negative values are silently ignored (defaults apply). Both `cost.ts` and `loop.ts` now delegate to these functions instead of using hardcoded constants.
