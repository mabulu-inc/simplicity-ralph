# T-091: Fix 15 stale and missing documentation items across README and docs-site

- **Status**: DONE
- **Milestone**: 12 — Documentation
- **Depends**: none
- **PRD Reference**: §3.1, §3.2, §3.12, §12
- **Complexity**: standard
- **Touches**: `README.md`, `docs-site/astro.config.mjs`, `docs-site/src/content/docs/**`
- **Completed**: 2026-03-19 16:18 (5m duration)
- **Commit**: 987737ccff79b78e6479de987cd89e16d71fc77e
- **Cost**: $0.74

## Completion Notes

Fixed all 15 stale and missing documentation items. Removed agent instructions file references from README and init/quick-start docs. Removed database references from init prompts, loop iteration cycle, loop options, and prompts template variables. Added missing exit conditions (budget exceeded, no eligible tasks) to loop docs. Added retry to sidebar navigation. Rewrote review --coach documentation to describe AI-powered coaching with both task-specific and project-wide modes. Verified reference pages exist. Added 19 tests validating all documentation changes.

## Description

An audit of the README and docs-site against the current PRD found 15 issues. Most are stale references from before the built-in-first architecture (T-076), database removal (T-088), and agent instructions independence (T-086). Some are incomplete documentation for features added in T-083/T-090.

## Changes

### README.md

1. **Remove agent instructions file from init scaffolding list** (line ~40) — ralph no longer generates `.claude/CLAUDE.md` or equivalent. The PRD explicitly states this in §11.5.

### docs-site: getting-started/quick-start.md

2. **Remove `docs/RALPH-METHODOLOGY.md`** from the scaffolding list — methodology lives in ralph's package code.
3. **Remove `docs/prompts/boot.md`** from the scaffolding list — boot.md is only a user extension file, not something init creates.
4. **Remove agent instructions file** from the scaffolding list.
5. **Add `ralph.config.json`** to the scaffolding list — it IS created by init but was missing.

### docs-site: commands/init.md

6. **Remove agent instructions file row** from the Generated Files table.
7. **Remove "Database"** from the prompts list — T-088 removed it from init.

### docs-site: commands/loop.md

8. **Remove "Database — start Docker containers"** step from the iteration cycle.
9. **Remove `--no-db` option** from the options table.
10. **Add missing exit conditions** — "loop budget exceeded" and "no eligible tasks" are in the PRD but missing from the docs-site loop page.

### docs-site: core-concepts/prompts.md

11. **Remove `{{config.database}}`** from the template variables table.

### docs-site: astro.config.mjs

12. **Add `retry` to the sidebar** — the page exists but isn't in the navigation.

### docs-site: commands/review.md

13. **Rewrite `--coach` description** — currently describes it as heuristic checks ("identifies tasks with missing acceptance criteria"). Must describe the AI-powered coaching per PRD §3.12: task-specific coaching sends execution data to the AI, project-wide coaching analyzes patterns with the AI.
14. **Add `ralph review T-NNN --coach`** documentation — task-specific coaching is a distinct mode from project-wide `--coach` and is not documented at all.

### Verification

15. **Check that reference/task-file-api.md and reference/extension-api.md exist** on disk in the docs-site. The sidebar has entries for them — verify the pages are present and contain the contract documentation from PRD §12.

## AC

- README no longer references agent instructions files as scaffolded by init
- Quick-start scaffolding list matches what init actually creates: `docs/PRD.md`, `docs/tasks/T-000.md`, `docs/prompts/rules.md`, `ralph.config.json`
- Init docs page has no database prompt and no agent instructions file
- Loop docs page has no database step, no `--no-db` flag, and lists all 5 exit conditions
- Prompts docs page has no `{{config.database}}` variable
- Sidebar includes `retry` command
- Review docs page describes AI-powered coaching (not heuristic) and documents both task-specific and project-wide modes
- Reference pages for Task File API and Extension API exist and match PRD §12
- `pnpm check` passes
