# T-070: Improve monitor labels and add progress bar to "This run"

- **Status**: DONE
- **Milestone**: 8 — Robustness
- **Depends**: none
- **PRD Reference**: §3
- **Complexity**: small
- **Completed**: 2026-03-18
- **Commit**: 1156df488692609107026c3545744d2d5e8c3361
- **Cost**: $0.41

## Description

The monitor dashboard currently shows:

```
Status: RUNNING
Progress: [████████████████░░░░] 88/96 (92%)
This run: 1/9 tasks completed
Spend: loop $77.96 / task $0.00
```

Two changes:

1. Rename the `Progress` label to `Overall` for clarity.
2. Change `This run` to use the same format as `Overall` — a progress bar with counts and percentage instead of plain text.

Target output:

```
Status: RUNNING
Overall: [████████████████░░░░] 88/96 (92%)
This run: [██░░░░░░░░░░░░░░░░░░] 1/9 (11%)
Spend: loop $77.96 / task $0.00
```

## Requirements

1. **Rename label** — change `Progress:` to `Overall:` in `formatMonitorOutput()`.
2. **Progress bar on "This run"** — update `formatRunProgress()` to call `formatProgressBar()` with the run-scoped counts (`completedThisRun` done, `remainingAtStart` total) and prefix with `This run:`.
3. **All existing tests must continue to pass** — update any snapshot or assertion that references the old label or format.

## Produces

- Updated `formatMonitorOutput()` in `src/ralph/commands/monitor.ts` — label change
- Updated `formatRunProgress()` in `src/ralph/commands/monitor.ts` — progress bar format
- Updated tests to match new output

## Completion Notes

Renamed `Progress:` label to `Overall:` in `formatMonitorOutput()` and changed `formatRunProgress()` to use `formatProgressBar()` instead of plain text. Both the "Overall" and "This run" lines now display a consistent progress bar format.
