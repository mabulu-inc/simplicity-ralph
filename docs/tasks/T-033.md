# T-033: Implement Codex CLI agent provider

- **Status**: TODO
- **Milestone**: 9 — Agent Abstraction
- **Depends**: T-031
- **PRD Reference**: §10.2

## Description

Implement the Codex CLI provider using the agent provider interface from T-031.

**Requirements:**

- Binary: `codex`
- Print mode: `exec` subcommand (prompt as positional argument)
- JSON output: `--json` (JSONL)
- Max turns: not supported — rely on ralph timeout
- Instructions file: `AGENTS.md`
- Parse Codex's JSONL stream into ralph's normalized event format
- Update `ralph init` to generate `AGENTS.md` when codex is the selected agent

## Produces

- `src/ralph/providers/codex.ts`
- Updated `src/ralph/commands/init.ts`
- Tests for the Codex provider
