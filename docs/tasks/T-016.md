# T-016: Task complexity scoring

- **Status**: DONE
- **Milestone**: 6 — Integration & Polish
- **Depends**: T-001
- **PRD Reference**: §3.2
- **Completed**: 2026-03-10 21:18
- **Commit**: 673a71a66e050170b655467a2bbff623e01aac82
- **Cost**: $0.74

## Description

Add a `computeTaskComplexity()` function that inspects a task file and returns a complexity tier: `light`, `standard`, or `heavy`. This is used by the loop command to auto-scale `--max-turns` and `--timeout` per iteration.

**Complexity signals (from the task file):**

| Signal              | How to detect                                                                   |
| ------------------- | ------------------------------------------------------------------------------- |
| Dependency count    | Number of entries in the `Depends` field                                        |
| Output file count   | Number of items in the `Produces` section                                       |
| Integration keyword | Title or description contains "integration", "end-to-end", "e2e", or "refactor" |

**Tier thresholds:**

| Tier     | Criteria                                       |
| -------- | ---------------------------------------------- |
| Light    | 0–1 deps, 1–2 produces, no integration keyword |
| Standard | 2–3 deps OR 3–4 produces                       |
| Heavy    | 4+ deps OR 5+ produces OR integration keyword  |

The function takes a parsed `Task` (from T-001's task parser) and returns the tier. No file I/O — it operates on the already-parsed task data.

## Produces

- `src/ralph/core/complexity.ts`
- `src/ralph/__tests__/complexity.test.ts`

## Completion Notes

Implemented `computeTaskComplexity()` in `src/ralph/core/complexity.ts` that returns `light`, `standard`, or `heavy` based on dependency count, produces count, and integration keywords (case-insensitive). Added `producesCount` field to the `Task` interface and parser in `tasks.ts` using indexOf-based section extraction. 18 tests covering all tiers, keyword matching (title and description), case insensitivity, and tier precedence. 100% coverage.
