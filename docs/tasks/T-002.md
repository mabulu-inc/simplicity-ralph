# T-002: Project config parser

- **Status**: DONE
- **Milestone**: 2 — Core Modules
- **Depends**: T-000
- **PRD Reference**: §2, §2.1, §2.2
- **Completed**: 2026-03-10 18:22 (2m duration)
- **Commit**: 1161bd940423cabfe5db49a3b60841fea6773757
- **Cost**: $0.51

## Description

Read project configuration from `.claude/CLAUDE.md`. Extract the structured config fields that ralph needs to operate.

**Capabilities:**

- Read a CLAUDE.md file and extract: language, package manager, testing framework, quality check command, test command
- Handle optional fields: database config, file naming convention
- Return a typed config object
- Validate that required fields are present (check command is mandatory for loop)

## Produces

- `src/ralph/core/config.ts`
- Tests covering: full config extraction, missing fields, optional fields, validation

## Completion Notes

Implemented project config parser in `src/ralph/core/config.ts` with `parseConfig()` and `readConfig()`. Extracts language, package manager, testing framework, quality check command, test command from the `## Project-Specific Config` section of CLAUDE.md. Optional fields: file naming, database. Commands in backticks are extracted cleanly. Validates all required fields are present. 12 tests covering full extraction, minimal config, backtick values, missing required fields, missing section, and file I/O. 100% line coverage.
